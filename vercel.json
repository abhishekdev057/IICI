{
  "buildCommand": "rm -rf node_modules/.prisma && rm -rf node_modules/@prisma && rm -rf node_modules/prisma && npm cache clean --force && npm install && npx prisma generate && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
